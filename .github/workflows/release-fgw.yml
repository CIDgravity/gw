name: Release fgw
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  release-fgw:
    if: ${{ vars.INTERNAL_CI_ENABLED }}
    uses: ./.github/workflows/release.yml
    with:
      context: '.'
      dockerfile: 'Dockerfile'
      service: 'fgw'
